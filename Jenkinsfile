pipeline {
    agent any
        parameters {
          string(defaultValue: "3", description: 'pass the name of rom', name: 'Rom')
          }
          stages {
            stage('my name'){
              steps{
                
                sh 'sudo my name is rom'
              }
            }
          }
         }
